var minhaVar = 'minha variável';

function minhFunc(x, y){
    return x + y;
}

// ES 6 ou ES 2015
let num = 2;
const PI = 3.14;

var numero = [1, 2, 3];
numero.map( function (valor) { return valor * 2 } );

numero.map( (valor) => valor * 2 );

class Matematica{
    soma(x, y){
        return x + y;
    }
}

var n1 = 'sdfasdf';
n1 = 4;